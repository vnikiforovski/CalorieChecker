apiVersion: policy/v1
kind: PodDisruptionBudget

metadata:
  name: backend-pdb
  namespace: caloriechecker-prod

  labels:
    app: backend
    tier: backend

spec:
  minAvailable: 1

  selector:
    matchLabels:
      app: backend